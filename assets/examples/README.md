# Example slots

Add accepted examples here after the first real design run. Keep the set representative of the pipeline:

1. Brand kit.
2. Screen reference.
3. Final package overview.

The README already contains the centered image layout. Add the files, then remove the surrounding HTML comment so GitHub renders them.
